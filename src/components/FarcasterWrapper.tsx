'use client';

import type { ReactNode } from 'react';

export default function FarcasterWrapper({ children }: { children: ReactNode }) {
  // Minimal placeholder wrapper. Replace with real Farcaster integration as needed.
  return <>{children}</>;
}
